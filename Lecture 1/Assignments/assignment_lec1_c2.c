/* Judelle Gaza - CS21 - Assignment 1; assignment_lec1_c2.c*/

#include <stdio.h>
int main(void)
{
    //Declaring variable types
    int a,b,result;             

    //Assigning values to a , b , and result
    a = 15;                      
    b = 87;
    result = b - a;

    //outputs the integers and the difference to the terminal
    printf("Subtracting %d from %d results to %d", a, b, result);  
    return 0;
}
